{
  "name": "build-number-action",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "npm run typecheck && ncc build src/main.ts -o dist --license licenses.txt"
  },
  "dependencies": {
    "@actions/core": "^2.0.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4",
    "typescript": "^5.9.3"
  }
}